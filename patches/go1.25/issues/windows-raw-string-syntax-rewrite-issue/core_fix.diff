diff --git a/instrument/instrument_var/trap_var.go b/instrument/instrument_var/trap_var.go
index f780f1c..d66bb24 100644
--- a/instrument/instrument_var/trap_var.go
+++ b/instrument/instrument_var/trap_var.go
@@ -60,7 +60,7 @@ func rewriteVarDefAndRefs(fset *token.FileSet, pkgPath string, file *edit.File,
 		config_debug.OnRewriteVarDefAndRefs(pkgPath, file, decl)
 	}
 	fileEdit := file.Edit
-	end := decl.Decl.End()
+	end := safeGenDeclEnd(file, decl)
 	// TODO: check if the end is a semicolon
 	// `;;` causes error
 	// endOffset := fset.Position(end).Offset
@@ -107,8 +107,7 @@ func rewriteVarDefAndRefs(fset *token.FileSet, pkgPath string, file *edit.File,
 		Decl:    decl,
 	})
 
-	fileEdit.Insert(end, ";")
-	fileEdit.Insert(end, code)
+	fileEdit.Insert(end, "\n"+code+"\n")
 
 	// apply edits for all refs
 	// from main module
@@ -118,6 +117,16 @@ func rewriteVarDefAndRefs(fset *token.FileSet, pkgPath string, file *edit.File,
 	return true
 }
 
+func safeGenDeclEnd(file *edit.File, decl *edit.Decl) token.Pos {
+	syntaxFile := file.File.Syntax
+	for i, d := range syntaxFile.Decls {
+		if d == decl.Decl {
+			return patch.GenDeclSafeEnd(syntaxFile, i)
+		}
+	}
+	return decl.Decl.End()
+}
+
 func applyRewrite(prefix string, varRef *edit.VarRef) {
 	fileEdit := varRef.File.Edit
 	if varRef.Addr != nil && !varRef.FieldAccess {
diff --git a/instrument/patch/init.go b/instrument/patch/init.go
index 6804ca9..9022332 100644
--- a/instrument/patch/init.go
+++ b/instrument/patch/init.go
@@ -4,44 +4,58 @@ import (
 	"fmt"
 	"go/ast"
 	"go/token"
+	"strings"
 
 	"github.com/xhd2015/xgo/support/edit/goedit"
 )
 
-// NOTE: new func is always inserted after a position
-// because a function or variable may have some
-// special directive like go:embed, go:linkname...
-// we cannot add anything before them.
 func GetFuncInsertPosition(file *ast.File) token.Pos {
-	// find first non-import declaration
-	for _, decl := range file.Decls {
+	for i, decl := range file.Decls {
 		genDecl, ok := decl.(*ast.GenDecl)
 		if !ok {
 			return decl.End()
 		}
 		if genDecl.Tok != token.IMPORT {
-			return genDecl.End()
+			return GenDeclSafeEnd(file, i)
 		}
 	}
 
 	fileEnd := file.End()
 	packageEnd := file.Name.End()
 	if fileEnd == packageEnd {
-		// packageEnd is usually used to import instrumented packages
-		// if there are other imports, it is not safe to declare
-		// a new function.
-		// so always insert a new line to begin the function.
-		//
-		// however, in reality if there is only one import,
-		// there is nothing to instrument actually.
-		// so we panic here
 		panic("nothing to instrument with empty file")
 	}
 	return fileEnd
 }
 
+func GenDeclSafeEnd(file *ast.File, declIndex int) token.Pos {
+	if declIndex+1 < len(file.Decls) && !HasGoDirective(file.Decls[declIndex+1]) {
+		return file.Decls[declIndex+1].Pos()
+	}
+	return file.Decls[declIndex].End()
+}
+
+func HasGoDirective(decl ast.Decl) bool {
+	var doc *ast.CommentGroup
+	switch d := decl.(type) {
+	case *ast.GenDecl:
+		doc = d.Doc
+	case *ast.FuncDecl:
+		doc = d.Doc
+	}
+	if doc == nil {
+		return false
+	}
+	for _, c := range doc.List {
+		if strings.HasPrefix(c.Text, "//go:") {
+			return true
+		}
+	}
+	return false
+}
+
 func Append(edit *goedit.Edit, file *ast.File, s string) {
-	edit.Insert(file.End(), s)
+	edit.Buffer().Insert(len(edit.Buffer().Old()), s)
 }
 
 func AddImport(edit *goedit.Edit, file *ast.File, name string, pkgPath string) {
@@ -49,5 +63,5 @@ func AddImport(edit *goedit.Edit, file *ast.File, name string, pkgPath string) {
 }
 
 func AddCode(edit *goedit.Edit, pos token.Pos, code string) {
-	edit.Insert(pos, ";"+code)
+	edit.Insert(pos, "\n"+code+"\n")
 }
diff --git a/runtime/test/build/instrument_raw_string/go.mod b/runtime/test/build/instrument_raw_string/go.mod
new file mode 100644
index 0000000..495b6f3
--- /dev/null
+++ b/runtime/test/build/instrument_raw_string/go.mod
@@ -0,0 +1,7 @@
+module github.com/xhd2015/xgo/runtime/test/build/instrument_raw_string
+
+go 1.18
+
+require github.com/xhd2015/xgo/runtime v0.0.0
+
+replace github.com/xhd2015/xgo/runtime => ../../..
diff --git a/runtime/test/build/instrument_raw_string/hello.go b/runtime/test/build/instrument_raw_string/hello.go
new file mode 100644
index 0000000..0068ddd
--- /dev/null
+++ b/runtime/test/build/instrument_raw_string/hello.go
@@ -0,0 +1,10 @@
+package instrument_raw_string
+
+const helpText = `
+Usage:
+  -func <pattern>    match function
+`
+
+func Hello() string {
+	return "hello"
+}
diff --git a/runtime/test/build/instrument_raw_string/hello_test.go b/runtime/test/build/instrument_raw_string/hello_test.go
new file mode 100644
index 0000000..4f5847b
--- /dev/null
+++ b/runtime/test/build/instrument_raw_string/hello_test.go
@@ -0,0 +1,9 @@
+package instrument_raw_string
+
+import "testing"
+
+func TestHello(t *testing.T) {
+	if Hello() != "hello" {
+		t.Fatal("unexpected result")
+	}
+}
